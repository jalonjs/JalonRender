"use strict";
function render(element, data) {
    var tpl = document.getElementById(element).innerHTML,
        body = 'var p=[]; p.push("' + tpl.replace(/[\r\n\t]/g, '').replace(/<%=(.*?)%>/g, '");p.push($1);p.push("').replace(/<%/g, '");').replace(/%>/g, 'p.push("') + '"); return p.join("");',
        exec = new Function('data', body);
    return exec(data)
}
